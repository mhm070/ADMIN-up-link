kind = "web"
previewPath = "/"
title = "Admin Link Pool"
version = "1.0.0"
id = "artifacts/admin-link-pool"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22788

[services.development]
run = "pnpm --filter @workspace/admin-link-pool run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/admin-link-pool", "run", "build" ]
publicDir = "artifacts/admin-link-pool/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22788"
BASE_PATH = "/"
